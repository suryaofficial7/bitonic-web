<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bitonic@Teacher | Upload</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
    integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />

  <link rel="shortcut icon" href="/images/fav.png" type="image/x-icon">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/responsive.css">
  <link rel="stylesheet" href="/css/animation.css">


  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<style>
        header {
            background-color: #333;
            color: #fff;
            padding: 10px;
            text-align: center;
        }

        .container1 {
            width: 50%;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        #dropZone {
            border: 2px dashed #ccc;
            padding: 20px;
            border-radius: 8px;
            cursor: pointer;
        }

        #fileInput {
            display: none;
        }

        label {
            background-color: #4caf50;
            color: #fff;
            padding: 10px;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 10px;
        }

        label:hover {
            background-color: #45a049;
        }

        #fileName {
            margin-top: 10px;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            display: block;
            width: 50%;
            margin-top: 10px;
        }

        .btn:hover {
            background-color: #45a049 !important;
        }

            .info-container {
            width: 50%;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        p {
            font-size: 16px;
            line-height: 1.6;
            color: #333;
        }
    </style>

<body>
{{>teacherNav}}
    <header>
        <h1>Drag and Drop File Upload</h1>
    </header>

    <form  action="uploads" method="post" class="container1 m-w-100">
        <div id="dropZone" ondrop="dropHandler(event);" ondragover="dragOverHandler(event);">
            <p>Drag and drop a PDF or Word file here, or click to select one.</p>
            <input type="file" id="fileInput" name="fileInput" accept=".pdf, .doc, .docx" onchange="handleFileChange(this.files)">
            <label for="fileInput" class="btn btn-primary">Choose File</label>
            <span id="fileName">No file chosen</span>
            <br><br>
            <input type="text"  style="width: 80%;" name="fileName" placeholder="Ex: chapter5" required>

        </div>

        <button class="btn btn-primary" type="submit">Upload</button>
    </form>

<div class="container">
<center></center>
    <ul>
       <code> <li class="code"> ✅ Only for Teachers </li></code>
       <code> <li class="code"> ✅ Tetachers Can Share only Pdf and Docx</li></code>
       <code> <li class="code"> ✅ Files Should Not be More Than 50.00Mb </li></code>
       <code> <li class="code"> ✅ <a href="yourUploads" style="text-decoration-line: underline !important; "> View All Your Uploads </a></li></code>

    </ul>
</div>

 
    <script>
        function handleFileChange(files) {
            var fileName = files[0].name;
            document.getElementById('fileName').innerHTML = fileName;
        }

        function dropHandler(event) {
            event.preventDefault();
            var files = event.dataTransfer.files;
            handleFileChange(files);
        }

        function dragOverHandler(event) {
            event.preventDefault();
        }
    </script>
{{>noFooter}}
</body>
</html>
